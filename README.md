# GraphQL SQL Node Server

GraphQL SQL Node Server

## Commands

```sh
# initial
yarn
yarn sequelize db:create

# run server locally
yarn dev

# create migration
npx sequelize migration:generate -n MigrationName
```
